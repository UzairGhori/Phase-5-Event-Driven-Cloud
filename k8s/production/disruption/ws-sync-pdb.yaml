# Task: T-E004 — PDB for WebSocket Sync Service
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: ws-sync-pdb
  namespace: todo-app
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: ws-sync-service
