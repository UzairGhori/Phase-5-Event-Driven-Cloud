# Task: T-E005 — TLS Certificate
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: todo-app-tls
  namespace: todo-app
spec:
  secretName: todo-app-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - todo.example.com  # Replace with actual domain
