# Task: T-E002 — Production Audit Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-service
  namespace: todo-app
spec:
  replicas: 1
  strategy: { type: RollingUpdate, rollingUpdate: { maxSurge: 1, maxUnavailable: 0 } }
  selector:
    matchLabels:
      app: audit-service
  template:
    metadata:
      labels:
        app: audit-service
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "audit-service"
        dapr.io/app-port: "8004"
        dapr.io/app-protocol: "http"
        dapr.io/enable-metrics: "true"
        dapr.io/metrics-port: "9090"
    spec:
      containers:
        - name: audit-service
          image: ghcr.io/todo-app/todo-audit-service:IMAGE_TAG
          imagePullPolicy: Always
          ports: [{ containerPort: 8004 }]
          env:
            - name: DATABASE_URL
              valueFrom: { secretKeyRef: { name: db-secrets, key: connection-string } }
            - name: SECRET_KEY
              valueFrom: { secretKeyRef: { name: jwt-secrets, key: secret-key } }
            - { name: DAPR_HTTP_PORT, value: "3500" }
          livenessProbe: { httpGet: { path: /api/health, port: 8004 }, initialDelaySeconds: 10, periodSeconds: 15 }
          readinessProbe: { httpGet: { path: /api/ready, port: 8004 }, initialDelaySeconds: 5, periodSeconds: 10 }
          resources: { requests: { memory: "128Mi", cpu: "100m" }, limits: { memory: "256Mi", cpu: "250m" } }
---
apiVersion: v1
kind: Service
metadata: { name: audit-service, namespace: todo-app }
spec:
  selector: { app: audit-service }
  ports: [{ port: 8004, targetPort: 8004 }]
