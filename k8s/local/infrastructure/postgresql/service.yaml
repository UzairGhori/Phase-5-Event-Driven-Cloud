# Task: T-D003 — PostgreSQL Service
apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: todo-app
spec:
  selector:
    app: postgresql
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
